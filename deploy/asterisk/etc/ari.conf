; ARI is enabled for future call control; the control plane currently
; observes and originates over AMI. Credentials are dev defaults.
[general]
enabled = yes
allowed_origins = *

[santral]
type = user
read_only = no
password = santral-ari-secret
password_format = plain
